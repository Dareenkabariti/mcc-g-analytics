<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mygoogleanalystics">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainSmartTvActivity"></activity>
        <activity android:name=".MainIphoneActivity" />
        <activity android:name=".MainLptopsActivity" />
        <activity android:name=".MainBagsActivity" />
        <activity android:name=".MainShoesActivity" />
        <activity android:name=".MainDressesActivity" />
        <activity android:name=".MainDessertsActivity" />
        <activity android:name=".MainDrinksActivity" />
        <activity android:name=".MainMealsActivity" />
        <activity android:name=".MainClothesActivity" />
        <activity android:name=".MainElectronicsActivity" />
        <activity android:name=".MainFoodActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>